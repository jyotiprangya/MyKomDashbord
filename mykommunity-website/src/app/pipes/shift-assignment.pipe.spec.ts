import { ShiftAssignmentPipe } from './shift-assignment.pipe';

describe('ShiftAssignmentPipe', () => {
  it('create an instance', () => {
    const pipe = new ShiftAssignmentPipe();
    expect(pipe).toBeTruthy();
  });
});
