import { FiltercomplaintPipe } from './filtercomplaint.pipe';

describe('FiltercomplaintPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercomplaintPipe();
    expect(pipe).toBeTruthy();
  });
});
