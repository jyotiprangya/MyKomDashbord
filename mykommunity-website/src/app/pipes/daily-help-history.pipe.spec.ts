import { DailyHelpHistoryPipe } from './daily-help-history.pipe';

describe('DailyHelpHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new DailyHelpHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
