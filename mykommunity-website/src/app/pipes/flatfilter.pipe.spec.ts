import { FlatfilterPipe } from './flatfilter.pipe';

describe('FlatfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FlatfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
